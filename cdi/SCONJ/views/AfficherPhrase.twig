{% extends 'BaseTemplate.twig' %}

{% block content1 %}
<body>
    <section class="Jeu" id="Jeu">
        {% set phraseLength = objetphrase|length %}
        {% for i in 0..0 %}
        <div class="temps_niveau">
            <p>Temps:&nbsp;{{ objetphrase[i].conju_temps }}</p>
        </div>
        <div class="phrase" id="phrase">
            <div id="avant">
                <p id="phraseTest">{{ phraseTps }}</p><span id="hideVerbe"></span><p id ="phraseTest">{{ phraseTpsAfter }}</p>
            </div>
            <div id="apres">
                <p> {{ phrasecomplete }} </p>
            </div>
        </div>
        <div class="reponses">
            <span id="reponse" hidden>{{ reponse }}</span>
            {% for dataRep in reponses %}
            <button  class="RepBtn" id="{{ dataRep }}">{{ dataRep }}</button>
            {% endfor %}
        </div>

        <div class="suivant">
            {% for phrasesuivante in objetphrase %}
            <button id="Next" class="">Suivant</button>
            {% endfor %}
            
        </div>

        <div class="nombrePhrases">
            <p>1/5</p>
        </div>
        <div class="click">
            <span id="nombreClick"></span>
        </div>
        {% endfor %}
    </section>
        <div id="phrasesuivante"></div>
        
    <script src="./views/templates/js/MainPhrase.js"></script>
</body>
{% endblock %}